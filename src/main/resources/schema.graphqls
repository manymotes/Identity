type Query {
    getUserById(id: ID!): UserResponse
}

type Mutation {
    createUser(user: UserInput!): UserResponse!
}
#todo move create to mutations

type User {
    uuid: ID!
    password: String!
    email: String!
    firstName: String!
    lastName: String!
}

type UserResponse {
    uuid: ID!
    email: String!
    firstName: String!
    lastName: String!
}

input UserInput {
    password: String!
    email: String!
    firstName: String!
    lastName: String!
}
